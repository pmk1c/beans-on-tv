import perfectSize from '../perfectSize';

const spacing = {
  '2xs': perfectSize(4),
  xs: perfectSize(8),
  s: perfectSize(16),
  m: perfectSize(24),
  l: perfectSize(32),
  xl: perfectSize(40),
  '2xl': perfectSize(48),
};

export default spacing;
